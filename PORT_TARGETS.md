# Port targets — `zip-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `zip` that codex-rs uses, and therefore the minimum subset that `zip-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-197] (rank `192`, tier `direct`, status `To Do`)

**Cargo package name**: `zip`
**Rust module name (in `use` statements)**: `zip`

## Summary

- **3** distinct symbols imported by codex-rs
- **3** total `use` statements
- **2** codex source files affected
- **1** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `zip::ZipArchive`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core-plugins`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core-plugins/src/startup_sync.rs:14` (`codex-core-plugins`) — `use zip::ZipArchive`

### `zip::ZipWriter`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core-plugins`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core-plugins/src/startup_sync_tests.rs:12` (`codex-core-plugins`) — `use zip::ZipWriter`

### `zip::write::SimpleFileOptions`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-core-plugins`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `core-plugins/src/startup_sync_tests.rs:13` (`codex-core-plugins`) — `use zip::write::SimpleFileOptions`
